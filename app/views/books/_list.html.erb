<div>
<h2>User info</h2>
  <%= image_tag user_new.get_profile_image(100,100) %>
  <% if user == current_user %>
    <table class = "table" >
        <tbody>
            <tr>
                <th>name</th>
                <th><%= user.name %></th>
            </tr>
            <tr>
                <th>introduction</th>
                <th><</th>
            </tr>
        </tbody>
    </table>


  <% end %>
  <p><%= link_to "プロフィール編集", edit_user_path(user) %></p>
  </div>

  <table>
 <thead>
  <tr>
   <th></th>
   <th>本のタイトル</th>
   <th>感想</th>
   <th colspan="3"></th>
  </tr>
 </thead>

 <tbody>
 <% books.each do |book| %>
  <tr>
    <td>
      <a href='users.id'></a>
    </td>
   <td>
   <span><%= link_to book.title, "/books/#{book.id}" %></span>
   </td>
   <td>
   <%= book.body %>
   </td>


  </tr>

 <% end %>
 </tbody>
</table>

<h1>NewBook</h1>


  <%= form_with model: book_new do |f| %>
    <h4>本のタイトル</h4>
      <%= f.text_field :title %>
    <h4>感想</h4>
      <%= f.text_area :body %>
      <%= f.submit 'create book' %>
  <% end %>
